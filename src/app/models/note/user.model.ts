export class User {
  constructor(
    public name?: string,
    public email?: string,
    public photoUrl?: string,
    public uid?: string,
    public displayName?: string,
    public emailVerified?: boolean,
  ){

  }
}
